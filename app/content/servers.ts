/**
 * Server shown in the Home status panel when the selected instance has no
 * servers of its own. Set `address` to an empty string to hide the panel instead.
 */
export const FEATURED_SERVER = {
  name: 'Elysia SMP',
  address: 'elysiasmp.com',
}
