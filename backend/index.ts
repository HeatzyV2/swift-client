/**
 * Pterodactyl entry — MAIN_FILE = `index.ts`
 * Egg runs: ts-node --esm /home/container/index.ts
 *
 * Real code is compiled to dist/ on `npm install` (postinstall).
 */
import './dist/index.js'
